## 用户偏好与协作要求

- 默认使用中文回复用户；专有名词、代码标识、命令、文件名保留英文。
- 项目内面向用户的界面、按钮、提示、报错、文档说明优先使用中文。
- 回复要直接说明结论、实际改动、原因、验证结果，避免空泛过程描述。
- 不使用 Codex worktree；所有改动都在 `/Users/hypered/Github/Insider-Monitor` 主目录完成。
- 不把 RPC URL、Telegram token、Discord webhook 等密钥写进示例配置或提交内容；使用 `.env`。
- 交付前运行可用验证；发现问题先修好再汇报。
